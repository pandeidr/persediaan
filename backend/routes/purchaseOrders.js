const express = require('express');
const router = express.Router();

// TODO: Implement purchaseOrders routes

router.get('/', (req, res) => {
  res.json({ message: 'purchaseOrders endpoints - coming soon' });
});

module.exports = router;
